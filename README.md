# Fluente-Mente

Repository GitHub Pages/Jekyll data-driven per il sistema Fluente-Mente.

## Pipeline

`_data/*.yml` → `validate_yml.py` → `generate_site.py` → Jekyll → GitHub Pages

GitHub Actions esegue automaticamente la validazione, la generazione delle pagine, il build e il deploy.
